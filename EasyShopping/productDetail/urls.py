from django.urls import path
from .views import *
urlpatterns = [
    # # path('products/<int:id>/', views.ProductDetailView.as_view()),
    # path("products/<pid>/", product_detail_view, name="product-detail"),
    # path('add-to-cart/<productID>/<sizeID>', add_to_cart, name='add_to_cart'),
]
